``

```
artisan migrate
artisan schedule:work
artisan queue:work --timeout=1000 --tries=3 --memory=1024
```

# Artisan commands
```
wb-upload
wb-upload:detail-report  Wildberries api detail-report prepare jobs and call their for upload data
wb-upload:income         Wildberries api incomes prepare jobs and call their for upload data
wb-upload:order          Wildberries api orders prepare jobs and call their for upload data
wb-upload:price          Wildberries api prices prepare jobs and call their for upload data
wb-upload:sale           Wildberries api sales prepare jobs and call their for upload data
wb-upload:stock          Wildberries api stocks prepare jobs and call their for upload data
```


Queue Monitor http://localhost/jobs
